# Cloudflare Pages Redirects
# This file ensures proper handling of routes and 404 errors

# Redirect all 404s to index.html (for single-page app behavior)
# This ensures that direct URL access or refresh works correctly
/*    /index.html   200

# Optional: Add specific redirects if needed in the future
# Example: /old-page    /new-page    301

